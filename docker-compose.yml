services:
  hermy-hq-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hermy-hq-dashboard
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production
      # Connect to external hermy-postgres on hermy-infra network
      - DATABASE_URL=postgresql://hermy:xdEHzjIO5TruRa9LFxYS36vYBLvwzQyVZi%2BamYW2ok0%3D@hermy-postgres:5432/hermy_hq
      - NEXTAUTH_SECRET=change-me-in-production
      - INTERNAL_API_SECRET=hermes-internal-secret-change-me
    networks:
      - default
      - hermy-infra
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/hermes/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  default:
  hermy-infra:
    external: true

volumes:
  hermes-data:
